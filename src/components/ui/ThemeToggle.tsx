// Dark mode is disabled — the app is locked to light, so the theme switch is
// hidden. Kept as a no-render component so existing usages compile untouched.
export function ThemeToggle() {
  return null;
}
